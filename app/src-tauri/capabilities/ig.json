{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "ig-remote",
  "description": "A janela do Instagram (URL remota) emite o resultado do fetch de volta pro backend via evento. Same-origin, so o proprio /api/v1 do IG.",
  "windows": ["ig"],
  "remote": {
    "urls": ["https://*.instagram.com", "https://instagram.com"]
  },
  "permissions": ["core:event:default"]
}
